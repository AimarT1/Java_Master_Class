package com.javamasteclass;

public class privateClass {
    // package private class : class privateClass. aka you remove the public keyword.
    //package private interface: interface privateClass. aka you remove the public keyword.

    //protected- the object is visiable anywhere in its own package(like package private)
    // but also in subclasses even if they are in another package.

    public privateClass() {
    }
}
